const sam=process.argv.slice(2);
for (const get of sam) {
    console.log(`Hello ${get} from sam`);
}
const getlot=process.argv.slice(2);
const sangola=process.argv.slice(2);
console.log(getlot);
console.log(sangola);
