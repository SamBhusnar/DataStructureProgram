#include<stdio.h>
int main(){
    // write a programe for multiplication of two floating numbers
    float a,b;
    printf("Enter two any number \n");
    scanf("%f%fs",&a,&b);
    printf("Multiplication of two floating number is %f",(a*b));
    return 0;
}