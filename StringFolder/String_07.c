#include <stdio.h>
#include <string.h>
int main()
{
    // we already saw some string method that is strlen(); strcat();
    // we are now see remaining all that is strcpy() strcpr
    // char ptr1[] = "SangolaCollegSangola";

    // char ptr2[] = "Samadhan";
    // char ptr1[]="sangolc";
    // char ptr2[]="sangolb";
    
    printf("We are going to see some string method here\n ");
    // printf("Strcpy\n : %s",strcpy(ptr1,ptr2));
    // printf("Strcpy appled :%s \n ",strcpy(ptr1,ptr2));
    // puts(ptr1); if there is pointer string then this method not work there
    // now we are going to see strcmp
    printf("We are going to see strcmp : %d ",strcmp(ptr1,ptr2));
    return 0;
}