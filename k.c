#include<stdio.h>
int main(){
    printf("We learn about operator in c\n");
    printf("We learn about format specifier in c\n");
    int x;
    // %c%f%d//these is %c for charactor %f for floating number %d for intger number
   x=9;
   //upcocming symbol for formate specifire like that [%d %c %f] these is formate specifier
   
    printf("The value of x is %d\n",x);
    printf("The value of x is %d\n",x);
    //a-- first asign and then minus  operation with one
    //a++ fist asing and then plus  operation with one 
    //--a fist minus and then asing  operation with one
    //++a fitst plus and asing   operation with one 

    return 0;
}